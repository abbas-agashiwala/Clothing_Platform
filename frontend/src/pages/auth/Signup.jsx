import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../../context/AuthContext';export default function Signup(){const[f,setF]=useState({name:'',email:'',phone:'',password:'',confirmPassword:''}),{register}=useAuth(),nav=useNavigate();const go=async e=>{e.preventDefault();try{await register(f);nav('/home')}catch(e){alert(e.response?.data?.message||'Signup failed')}};return <div className="auth-wrap"><form className="card p-4 shadow auth-card" onSubmit={go}><h2>Create Account</h2>{['name','email','phone','password','confirmPassword'].map(k=><input key={k} className="form-control mb-3" required type={k.includes('password')||k==='confirmPassword'?'password':k==='email'?'email':'text'} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>)}<button className="btn btn-dark w-100">Signup</button><p className="mt-3">Already have an account? <Link to="/login">Login</Link></p></form></div>}