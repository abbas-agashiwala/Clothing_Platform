import{useState}from'react';import{Link,useNavigate}from'react-router-dom';import{useAuth}from'../../context/AuthContext';export default function Login(){const[f,setF]=useState({email:'',password:''}),{login}=useAuth(),nav=useNavigate();const go=async e=>{e.preventDefault();try{const u=await login(f);nav(u.role==='ADMIN'?'/admin/dashboard':'/home')}catch(e){alert(e.response?.data?.message||'Login failed')}};return <div className="auth-wrap"><form className="card p-4 shadow auth-card" onSubmit={go}><h2>Login</h2><input className="form-control mb-3" placeholder="Email" type="email" required value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input className="form-control mb-3" placeholder="Password" type="password" required value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button className="btn btn-dark w-100">Login</button><p className="mt-3">New user? <Link to="/signup">Create account</Link></p></form></div>}